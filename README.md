```
        /* String product1 = "Computer";
        String product2 = "Office desk";


        int age= 30;
        int code = 5290;
        char gender = 'F';

        double price1 = 2100.0;
        double price2 = 650.50;
        double measure = 53.234567;

        System.out.printf("Products:%n %s, which price is %f%n %s, which price is %f%n %n", product1,price1,product2,price2);
        System.out.printf("Record:%n %d years old, code %d and gender: %s %n %n",age,code,gender);
        System.out.printf("Measure with eight decimal places: %.7f %n",measure);
        System.out.printf("Rounded (three decimal places): %.3f %n",measure);
        Locale.setDefault(Locale.US);
        System.out.printf("Us decimal point:  %.3f", measure);
        */
```

```
// TODO: Faça um programa para ler o valor do raio de um círculo, e depois mostrar o valor da área deste círculo com quatro
//      casas decimais. Considere que o raio do círculo é dado por um número inteiro.
//      Fórmula da área: area = π . raio2
//      Considere o valor de π = 3.14159.

/*     int a = sc.nextInt();
       int b = sc.nextInt();
       int resultado = a + b;
       System.out.println("SOMA = " + resultado);
       double a, pi, raio;
       raio = sc.nextDouble();
       pi = 3.14159;
       a = pi * Math.pow(raio, 2);
       System.out.printf("A = %.4f " , a );

 */

//TODO: Fazer um programa para ler quatro valores inteiros A, B, C e D. A seguir, calcule e mostre a diferença do produto
//        de A e B pelo produto de C e D segundo a fórmula: DIFERENCA = (A * B - C * D).

   /*     int A,B,C,D, DIFERENCIA;
        A = sc.nextInt();
        B = sc.nextInt();
        C = sc.nextInt();
        D = sc.nextInt();

        DIFERENCIA = (A*B-C*D);
        System.out.println("DIFERENCIA = " + DIFERENCIA);
*/

        // TODO: Fazer um programa que leia o número de um funcionário, seu número de horas trabalhadas, o valor que recebe por
        //       dia e o salário do funcionário. O programa deve calcular e mostrar o salário total do funcionário.
        //       Considere que o salário do funcionário é dado por um número inteiro.
/*
        int numeroFuncionario, horasTrabalhadas;
        double salarioFuncionario, valorRecebidoPorHora;
        numeroFuncionario = sc.nextInt();
        horasTrabalhadas = sc.nextInt();
        valorRecebidoPorHora = sc.nextDouble();
        salarioFuncionario = horasTrabalhadas * valorRecebidoPorHora;
        System.out.println("NUMBER: " + numeroFuncionario);
        System.out.printf("SALARY = %.2f", salarioFuncionario);
*/
        // TODO: Fazer um programa para ler o código de uma peça 1, o número de peças 1, o valor unitário de cada peça 1, o
        //       valor total de todas as peças 1 e o valor unitário de cada peça 2. O programa deve calcular e mostrar o valor.
/*
        String codigoPeca1, codigoPeca2;
        int numeroPecas1, numeroPecas2;
        double valorUnitarioPeca1, valorUnitarioPeca2, valorTotal, numeroTotalPeca1, numeroTotalPeca2;

        codigoPeca1 = sc.next();
        numeroPecas1 = sc.nextInt();
        valorUnitarioPeca1 = sc.nextDouble();

        numeroTotalPeca1 = numeroPecas1 * valorUnitarioPeca1;

        codigoPeca2 = sc.next();
        numeroPecas2 = sc.nextInt();
        valorUnitarioPeca2 = sc.nextDouble();
        numeroTotalPeca2 = numeroPecas2 * valorUnitarioPeca2;

        valorTotal= numeroTotalPeca1 + numeroTotalPeca2;

        System.out.printf("VALOR A PAGAR: R$ %.2f",valorTotal);
*/


```

````
/*
// TODO: Fazer um programa para ler um número inteiro, e depois dizer se este número é negativo ou não.
  Scanner sc = new Scanner(System.in);
        int x = sc.nextInt();

        if (x == -10) {

            System.out.println("NEGATIVO");
        } else {
            System.out.println("NAO NEGATIVO");
        }


// TODO: Fazer um programa para ler um número inteiro e dizer se este número é par ou ímpar.
        Scanner sc = new Scanner(System.in);
        int x = sc.nextInt();
        if (x %2 == 0 ) {

            System.out.println("PAR");
        } else {
            System.out.println("IMPAR");
        }
// TODO: Leia 2 valores inteiros (A e B). Após, o programa deve mostrar uma mensagem "Sao Multiplos" ou "Nao sao
Multiplos", indicando se os valores lidos são múltiplos entre si. Atenção: os números devem poder ser digitados em
ordem crescente ou decrescente.

         Scanner sc = new Scanner(System.in);
int A,B;
A = sc.nextInt();
B = sc.nextInt();
if (A % B == 0 || B % A == 0) {
    System.out.println("SÃO MULTIPLOS");
}else {
    System.out.println("NÃO SÃO MULTIPLOS");
}

*/
```
````
